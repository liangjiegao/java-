<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.superl.skyDriver.mapper.UserRegisterMapper">

    <insert id="register" parameterType="com.superl.skyDriver.pojo.User">
        insert into user values (#{id}, #{username}, #{account}, #{passwd});
    </insert>

</mapper>